defmodule Fleetlm.Sessions.InboxReadPendingTest do
  use Fleetlm.DataCase

  @moduletag skip: "Read receipt runtime pending reintroduction"

  test "mark_read clears unread counts" do
    assert :todo == :todo
  end
end
